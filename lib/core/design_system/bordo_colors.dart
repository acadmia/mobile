import 'package:flutter/material.dart';

class BordoColors {
  // Cores de Fundo e Superfícies
  static const Color background = Color(0xFF0D0608); // Preto profundo
  static const Color surface = Color(0xFF1A0C11);    // Superfície escura translúcida

  // Cores de Destaque
  static const Color primary = Color(0xFF800020);    // Bordô clássico
  static const Color accent = Color(0xFFD9254C);     // Carmesim/Neon para botões ativos

  // Cores de Texto
  static const Color textPrimary = Color(0xFFF7F2F4);
  static const Color textSecondary = Color(0xFF8C7A80);
}
